# 1. Variáveis de ambiente corretas
$env:FLASK_APP = "back.app"
$env:DB_HOST = "127.0.0.1"
$env:DB_USER = "root"
$env:DB_PASS = "rootpass"
$env:DB_NAME = "appdb"



# 2. Inicializar o banco
python back/scripts/init_db.py

# 3. Subir a API (modo dev)
flask run --host=0.0.0.0 --port=8085
